environment:
  node_version: "4"

install:
  - ps: Install-Product node $env:node_version
  - choco install yarn
  - refreshenv
  - yarn global add webpack
  - yarn

test_script:
  - yarn test

build: off
